# ─── Core UI ──────────────────────────────────────────────────────────────────
# 1.51.0 is minimum version supporting Pillow>=12 (required for GHSA-cfh3-3jmp-rvhc patch)
streamlit==1.51.0

# ─── Ollama / OpenAI-compatible client ───────────────────────────────────────
openai==1.68.2

# ─── PDF → image conversion (PyMuPDF — import name: fitz) ────────────────────
pymupdf==1.25.3

# ─── Image processing ────────────────────────────────────────────────────────
# 12.1.1 patches GHSA-cfh3-3jmp-rvhc (out-of-bounds write on PSD load, high severity)
Pillow==12.1.1

# ─── Data manipulation and Excel export ──────────────────────────────────────
pandas==2.2.3
openpyxl==3.1.5          # pandas xlsx engine

# ─── PDF report generation ───────────────────────────────────────────────────
reportlab==4.2.5

# ─── Environment variable loading (local dev) ────────────────────────────────
python-dotenv==1.0.1

# ─── Tesseract OCR fallback (system tesseract-ocr must also be installed) ────
pytesseract==0.3.13
